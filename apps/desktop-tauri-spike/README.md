# Tauri Desktop Spike

This directory preserves the Tauri, React, Rust, and xterm.js scaffold as an experiment.

The primary product direction is now the all-Zig desktop host in `apps/desktop`. Keep this spike only for reference while the Zig-native app catches up on UI shape and PTY lifecycle details.

